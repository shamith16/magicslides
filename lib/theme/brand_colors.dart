import 'package:flutter/material.dart';

/// Centralized palette derived from the brand logo.
class BrandColors {
  BrandColors._();

  // Primary brand yellows
  static const Color primary = Color(0xFFF5B400);
  static const Color primaryDark = Color(0xFFD38F00);
  static const Color secondary = Color(0xFFFFCE53);

  // Neutral surfaces
  static const Color background = Color(0xFFFFFFFF);
  static const Color surface = Color(0xFFF7F7F7);
  static const Color outline = Color(0xFFE4E4E7);

  // Text / icon colors
  static const Color onPrimary = Color(0xFF1F1B15);
  static const Color onSurface = Color(0xFF2B2B2B);

  // Semantic colors
  static const Color success = Color(0xFF2E7D32);
  static const Color warning = Color(0xFFF57C00);
  static const Color error = Color(0xFFD32F2F);
}

